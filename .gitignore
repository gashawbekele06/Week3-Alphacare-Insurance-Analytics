# .gitignore
# 1. Ignore DVC's internal cache and state (Created by dvc init)
/dvc/cache
.dvc/tmp
.dvc/plots

# 2. Ignore ALL large files/directories tracked by DVC
# This keeps data out of the Git repo!
data/

# 3. EXCLUDE the DVC metadata files for Git tracking (The crucial part!)
!data/*.dvc

# Other standard ignores
__pycache__/
*.pyc
/venv
/env
/models  # If models are large and DVC-tracked